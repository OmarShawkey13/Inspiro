import 'package:flutter/material.dart';

class ColorsManager {
  // Primary Branding Colors
  static const Color primaryColor = Color(0xFF3E64FF); // Calm blue
  static const Color primaryDarkColor = Color(0xFF1C1C1E); // Near black for dark mode

  // Background Colors
  static const Color scaffoldBackgroundColor = Color(0xFFF5F7FA); // Soft white
  static const Color scaffoldDarkBackgroundColor = Color(0xFF121212); // True dark

  // Card Colors (for quotes)
  static const Color cardColor = Color(0xFFFFFFFF); // White cards
  static const Color cardColorDark = Color(0xFF1E1E1E); // Dark cards

  // Text Colors
  static const Color textColor = Color(0xFF111111); // Main dark text
  static const Color textColorDark = Color(0xFFF0F0F0); // Light text for dark mode

  // Subtitle / Quote Author
  static const Color subtitleColor = Color(0xFF6C7A89); // Muted gray
  static const Color subtitleColorDark = Color(0xFFB0B0B0); // Muted gray light

  // Neutrals
  static const Color gray = Color(0xFFBDBDBD);
  static const Color darkGray = Color(0xFF8E8E93);
  static const Color lightGray = Color(0xFFE5E5EA);

  static const Color whiteColor = Color(0xFFFFFFFF);
  static const Color blackColor = Color(0xFF000000);
}
